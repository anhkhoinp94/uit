PRINT 'Creating tables...';

CREATE TABLE KHACHHANG (
    MAKH CHAR(4) PRIMARY KEY,
    HOTEN VARCHAR(40),
    DCHI VARCHAR(50),
    SODT VARCHAR(20),
    NGSINH SMALLDATETIME,
    DOANHSO MONEY,
    NGDK SMALLDATETIME
);

PRINT 'Table KHACHHANG created successfully.';

CREATE TABLE NHANVIEN (
    MANV CHAR(4) PRIMARY KEY,
    HOTEN VARCHAR(40),
    SODT VARCHAR(20),
    NGVL SMALLDATETIME
);

PRINT 'Table NHANVIEN created successfully.';

CREATE TABLE SANPHAM (
    MASP CHAR(4) PRIMARY KEY,
    TENSP VARCHAR(40),
    DVT VARCHAR(20),
    NUOCSX VARCHAR(40),
    GIA MONEY
);

PRINT 'Table SANPHAM created successfully.';

CREATE TABLE HOADON (
    SOHD INT PRIMARY KEY,
    NGHD SMALLDATETIME,
    MAKH CHAR(4),
    MANV CHAR(4),
    TRIGIA MONEY
);
-- CREATE TABLE HOADON (
--     SOHD INT PRIMARY KEY,
--     NGHD SMALLDATETIME,
--     MAKH CHAR(4),
--     MANV CHAR(4),
--     TRIGIA MONEY,
--     FOREIGN KEY (MAKH) REFERENCES KHACHHANG(MAKH),
--     FOREIGN KEY (MANV) REFERENCES NHANVIEN(MANV)
-- );
PRINT 'Table HOADON created successfully.';

CREATE TABLE CTHD ( SOHD INT, MASP CHAR(4), SL INT );
-- CREATE TABLE CTHD (
--     SOHD INT,
--     MASP CHAR(4),
--     SL INT,
--     PRIMARY KEY (SOHD, MASP),
--     FOREIGN KEY (SOHD) REFERENCES HOADON(SOHD),
--     FOREIGN KEY (MASP) REFERENCES SANPHAM(MASP)
-- );
PRINT 'Table CTHD created successfully.';